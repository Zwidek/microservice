server:
  port: 8888
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Zwidek/spring-cloud-config
